<!DOCTYPE html>
<html>
  <head>
    <title><%= yield :title %> :: <%= current_library ? current_library.name : 'bookcamping' %></title>
    <link href='http://fonts.googleapis.com/css?family=Kreon:300,700|Cousine' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body class="<%= controller.class %> <%= controller.action_name %>">
    <%= render 'layouts/flashes' %>
    <%= render 'layouts/header' %>
    <div class="container">
      <%# render 'banner' %>
      <div class="row">
        <div class="sidebar left span3">
            <div class="sidecontent">
              <%= yield :left %>
            </div>
          &nbsp;
        </div>
        <div class="main span7">
          <%= yield %>
        </div>
        <div class="sidebar right span2">
            <div class="sidecontent">
              <%= yield :right %>
            </div>
          &nbsp;
        </div>
      </div>
    </div>
    <%= render 'layouts/footer' %>
  </body>
</html>
