<% title current_library.name %>
<%= div_for current_library do %>
  <%= render 'header', library: current_library %>
  <%= render "dash_#{current_library.ltype}", 
    library: current_library %>
<% end %>

