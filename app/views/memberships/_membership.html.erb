<%= div_for membership do %>
  <%= link_to 'Quitar', membership, method: :delete, class: 'action', 
    data: {confirm: "¿Quieres quitar a #{membership.user.name} como madrina?", action: "delete-membership-#{membership.id}" } %>
  <%= link_to membership.user.name, membership.user %>
<% end %>
