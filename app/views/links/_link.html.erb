<%= div_for link do %>
  <%= link_to link.description, link.url, target: '_blank' %>
  <%= content_tag :span, "(#{link.mime_type})", 
    class: 'mime_type' if link.mime_type? %>
  <%= content_tag :span, "En #{link.host}", 
    class: 'host' if link.host? %>
<% end %>
