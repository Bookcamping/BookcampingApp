<% title reference.title %>
<%= render 'shelves/shelf_references', shelf: shelf %>

<%= content_for :description do %>
  <%= content_tag_for :section, reference, class: 'summary' do %>
  <h2><%= link_to reference.title, reference %></h2>
<%= license_icon_tag reference.license %>
<%= reference.date %>
<%= reference.authors %>
<% if reference.editor? %>
  / <%= reference.editor %>
    <% end %>
<div class="description"><%= reference.description %></div>
<% if reference.url? && reference.license.libre? %>
  <div class="download_url">
  <%= link_to("¿Lo quieres? ¡Lo tienes! <b>&dArr;</b> Descargar!".html_safe, reference.url, :class => 'important download') %>
</div>
  <% end %>
<div class="media">
  <%= render_media(reference.media) if reference.media? %>
</div>
<% end %>
<% end %>
